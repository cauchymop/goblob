language: android
jdk: oraclejdk7
env:
  matrix:
    - ANDROID_TARGET=android-23  ANDROID_ABI=armeabi-v7a

android:
  components:
    - build-tools-23.0.2

before_script:
  # Nothing for us

script: ./gradlew goblobBase:test